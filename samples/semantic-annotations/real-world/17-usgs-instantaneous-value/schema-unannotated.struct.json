{
  "$schema": "https://json-structure.org/meta/extended/v0/#",
  "$id": "https://schemas.example.org/semantic-annotations/real-world/17-usgs-instantaneous-value-unannotated",
  "$uses": ["JSONStructureValidation"],
  "name": "GageHeight",
  "description": "One instantaneous value from the USGS National Water Information System, the gage-height series that the agency publishes for its surface-water monitoring sites. Derived from the usgs-iv feeder schema published in the xRegistry catalogue. This is the same record without its semantic layer, derived mechanically from `schema.struct.json` beside it: every keyword contributed by an extension add-in has been removed, along with any definition that existed only to give an annotation something to point at. What remains is the shape of the record and the prose a consumer would have to read to make sense of it. Read it against the annotated schema to see what the annotations carry. Do not edit this file; regenerate it with `make-unannotated.py`.",
  "type": "object",
  "definitions": {
    "QualifierEnum": {
      "name": "QualifierEnum",
      "type": "string",
      "enum": ["P", "A", "e", "<", ">", "&"],
      "description": "USGS data-qualification letter. `P` and `A` are mutually exclusive; the remaining letters may accompany either."
    },
    "ExceptionEnum": {
      "name": "ExceptionEnum",
      "type": "string",
      "enum": ["Ice", "Bkw", "Ssn", "Zfl", "Eqp", "Mnt", "Rat", "Dis", "Fld", "Pr", "***"],
      "description": "USGS exception code explaining why no value is reported. The codes are not interchangeable: `Ice` and `Bkw` describe a physical condition at the site, `Eqp` and `Mnt` describe the instrument, and `Ssn`, `Dis` and `Pr` describe the operating regime of the series."
    }
  },
  "properties": {
    "site_no": {
      "type": "string",
      "description": "USGS site number identifying the monitoring location, from the `site_no` field. It is unique only within the publishing agency, so a stable join key is the pair of agency code and site number. The site is the location whose water surface this record describes."
    },
    "datetime": {
      "type": "datetime",
      "description": "Instant at which the gage sampled the water surface, from the `datetime` field. The upstream feed reports it in the site's local standard time with an offset; the publisher normalises it to a UTC-aware timestamp. Sites report on a fixed interval, most commonly every fifteen minutes, so a gap in the series is detectable rather than inferred."
    },
    "value": {
      "type": "double",
      "description": "Gage height in feet, from the `value` field. It is the height of the water surface above the local gage datum established for this site, not an elevation above a national vertical datum, so values from two sites are not comparable without the datum offsets published in the site file. The member is absent when `exception` explains why no reading exists."
    },
    "qualifiers": {
      "type": "array",
      "items": {
        "type": {
          "$ref": "#/definitions/QualifierEnum"
        }
      },
      "description": "Qualification letters attached to the reading, from the `qualifiers` field. The list decides whether the value may be used in an analysis: a value carrying `P` has not been reviewed and will change, a value carrying `A` has been approved and will not, and a value carrying `e` was estimated rather than read from the instrument."
    },
    "exception": {
      "type": {
        "$ref": "#/definitions/ExceptionEnum"
      },
      "description": "Exception code explaining the absence of `value`, from the `exception` field. It is present exactly when `value` is absent, and it distinguishes a reading the site could not take from a reading the site did not attempt."
    },
    "parameter_cd": {
      "type": "string",
      "const": "00065",
      "description": "USGS parameter code for the series, from the `parameter_cd` field. Upstream this code is the only statement of what the number means: `00065` is gage height in feet, `00060` is discharge in cubic feet per second, `00010` is water temperature in degrees Celsius, and a consumer that does not resolve it against the USGS parameter-code dictionary cannot interpret `value` at all. The `observedProperty` annotation on this record states the same fact in the schema, so a consumer no longer has to."
    },
    "timeseries_cd": {
      "type": "string",
      "description": "USGS time-series identifier, from the `timeseries_cd` field. A site may publish several series for one parameter code — a primary sensor and a backup, or sensors at different points on the structure — and this code is what separates them. It carries no meaning of its own and is not resolvable against a published dictionary."
    }
  },
  "required": ["site_no", "datetime", "qualifiers", "parameter_cd", "timeseries_cd"],
  "additionalProperties": false
}
